import 'package:flutter/material.dart';

class card_screens extends StatelessWidget {
   
  const card_screens({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('card_screens'),
      ),
    );
  }
}