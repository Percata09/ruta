import 'package:flutter/material.dart';

class alert_screens extends StatelessWidget {
   
  const alert_screens({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('alert_screens'),
      ),
    );
  }
}